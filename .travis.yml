language: go
dist: trusty

os:
  - linux

matrix:
  include:
    - go: 1.8.x
    - go: 1.9.2

before_install:
  - go vet ./...

install:
  - export CI=TRAVIS
  - go get ./...

script:
  - go test -timeout 20m -v ./...;